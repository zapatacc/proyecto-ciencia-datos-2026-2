# Starter — Clase 2

Estos archivos se copian al repositorio privado individual, dentro de:

```text
actividades/clase-02/
```

Modifica `actividad.py`; conserva `datos_viajes.py` como fuente de datos. Completa los cinco `TODO`, ejecuta el script y agrega verificaciones para los casos indicados en el notebook.

```bash
python actividades/clase-02/actividad.py
```

En macOS o Linux puede ser necesario usar `python3`. La práctica termina únicamente cuando la rama `feat/resumen-viajes` se integra mediante un PR que aparece como **Merged** y cerrado.
