# Starter — Clase 4

Copia `main.py` hacia `labs/trabajo-local/clase-04/` antes de editar. El starter rastreado permanece como fuente de sólo lectura. La carpeta de trabajo forma parte del proyecto `uv` de la raíz; no crees otro `pyproject.toml` dentro del laboratorio.

Desde la raíz del repositorio:

```bash
uv sync --locked
uv run fastapi dev
```

Completa los `TODO` de `main.py` y comprueba `/`, `/viajes/42?pasajeros=2` y `/docs`. La actividad no requiere rama, commit, push, PR ni entrega en Canvas.
